import React from 'react';
import ReactDOM from 'react-dom';
import App from './App.js';

const root = document.getElementById('root');
const rootRoot = ReactDOM.createRoot(root);

rootRoot.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
