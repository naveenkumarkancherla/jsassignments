function averageOfThree(a, b, c) {
    var sum = a + b + c;
    var average = sum / 3;
    return average;
  }
  
  var result = averageOfThree(4, 5, 6);
  console.log("The Average of three numbers: " + result);
  